local multiport = require("./multiport")

local dhcpServer = {}
local dhcpServerPort = 67
local assignedIPs = {}

local assignIP = function(requestPacket)
    local ip = {}

    return clientIPRequest
end